# Vault Recipe — دستورالعمل ویکی دانش پروژه (طرح‌یار)

یک «ویکی دانش» زنده و append-friendly کنار کد، در `docs/wiki/`، که به‌جای re-scan کردن کل کدبیس در هر مکالمه،
خلاصه‌ی entity ها (مدل‌ها، سرویس‌ها، صفحات) و concept ها (flowها، الگوها، تصمیم‌ها) را نگه می‌دارد.

## شروع هر مکالمه/سشن
1. اول `docs/wiki/overview.md` و `docs/wiki/index.md` را بخوان (هزینه: ≤۸۰۰ توکن). هرگز کل کدبیس را re-scan نکن.
2. فقط صفحات مرتبط با تسک فعلی را drill-down کن (`docs/wiki/entities/*.md`, `docs/wiki/concepts/*.md`).

## وقتی کد را تغییر دادی — قانون اکید
هر بار که یک فایل پروژه را Write/Edit می‌کنی (خارج از `docs/wiki/`):
1. تشخیص بده این تغییر روی کدام entity/concept اثر می‌گذارد.
2. صفحه‌ی مربوطه را در `docs/wiki/` به‌روز کن (فیلد جدید/حذف‌شده، امضای تابع، route جدید، dependency جدید، edge case).
3. اگر entity/concept کاملاً جدید و به‌اندازه کافی مهم است (≥۳ جای دیگر به آن لینک می‌دهند یا concept مستقل با حجم قابل‌توجه)، صفحه جدید بساز و در `docs/wiki/index.md` لینک بده.
4. یک خط در `docs/wiki/log.md` اضافه کن: `## [YYYY-MM-DD] update | <خلاصه تغییر>`.

تغییرات trivial (typo، فرمت، rename بدون تغییر امضا) نیاز به آپدیت ویکی ندارند.
اگر چند فایل در یک مکالمه تغییر کردند، آپدیت‌های ویکی را یک‌بار در پایان مکالمه انجام بده، نه بعد از هر فایل.

قوانین کامل ساختار، نام‌گذاری و لینک‌گذاری در [docs/wiki/schema.md](./docs/wiki/schema.md).

## Lint دوره‌ای (وقتی کاربر گفت "ویکی رو بازبینی کن")
1. orphan pages (بدون inbound link) — لینک بده یا حذف کن.
2. broken link — صفحه‌ی گمشده را بساز یا لینک را حذف کن.
3. تناقض بین صفحات — flag کن، نپرس، بهترین حدس را انتخاب کن و در log ثبت کن.
4. صفحات کهنه (کد مرتبط تغییر کرده ولی صفحه آپدیت نشده) — بازخوانی و sync.

هدف: کاهش توکن مصرفی با دانش فشرده و به‌روز — نه مستندسازی برای مستندسازی. اگر این قانون رعایت نشود، ویکی stale می‌شود و مکالمات بعدی به اطلاعات غلط می‌رسند.
